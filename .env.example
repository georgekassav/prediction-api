# Environment Configuration
# ========================
#
# This project uses environment-specific .env files:
#
# LOCAL DEVELOPMENT:
#   - Copy .env.example to .env.local
#   - The app loads .env.local when NODE_ENV=development (default)
#
# STAGING (Railway):
#   - Set NODE_ENV=staging in Railway environment variables
#   - See .env.staging.example for required variables
#   - Set all variables directly in Railway dashboard
#
# PRODUCTION (Railway):
#   - Set NODE_ENV=production in Railway environment variables
#   - See .env.production.example for required variables
#   - Set all variables directly in Railway dashboard
#
# IMPORTANT: Railway injects env vars at runtime - no .env files needed on server

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Server
NODE_ENV=development          # development | staging | production | test
PORT=3000                     # Server port (Railway sets this automatically)
FRONTEND_URL=http://localhost:3001  # Frontend URL for CORS

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/predictions

# Redis
REDIS_URL=redis://localhost:6379

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
